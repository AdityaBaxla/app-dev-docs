# Object

Object is a collection of key value pairs, when the keys store function they are called methods, otherwise they are called properties. To make an empty object, following is the syntax.

```js
let user = new Object(); // "object constructor" syntax
let user = {}; // "object literal" syntax
```
